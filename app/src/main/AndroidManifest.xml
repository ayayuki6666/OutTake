<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.shino.ordersystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name="android.shino.ordersystem.Activity.OrderActivity"
            android:label="@string/order_info"></activity>
        <activity
            android:name="android.shino.ordersystem.Activity.FoodActivity"
            android:theme="@style/FoodActivityTheme" />
        <activity android:name="android.shino.ordersystem.Activity.LoginActivity" />
        <activity android:name="android.shino.ordersystem.Activity.RegisterActivity" />
        <activity android:name="android.shino.ordersystem.Activity.WelcomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="android.shino.ordersystem.Activity.MainActivity">

        </activity>
    </application>

</manifest>